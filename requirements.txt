# MAT runtime dependencies (cross-platform: macOS Intel/Apple Silicon, Windows, Linux)
# Install with: pip install -r requirements.txt

numpy>=1.24
opencv-python>=4.8
pillow>=9.0
scipy>=1.9          # optional: temporal smoothing (MAT falls back gracefully without it)
psutil>=5.9         # optional: memory monitoring (MAT falls back gracefully without it)

# Optional: higher-quality DualTVL1 optical flow for mask propagation.
# Without it MAT automatically falls back to Farneback (built into opencv-python).
# NOTE: install INSTEAD of opencv-python, not alongside it:
# opencv-contrib-python>=4.8
